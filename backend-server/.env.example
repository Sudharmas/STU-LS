# Local-only port (Render injects PORT automatically)
SYNC_SERVER_PORT=8090
SYNC_SERVER_HOST=0.0.0.0
SUPABASE_DB_URL=
BACKEND_SERVER_BEARER_TOKEN=
# Optional CORS allow-list for browser origins, comma-separated.
# Leave blank to allow all origins.
BACKEND_SERVER_CORS_ORIGINS=
